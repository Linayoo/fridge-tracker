# Copy this file to .env.local and set EXPO_PUBLIC_API_URL to your laptop's LAN IP.
# localhost won't work from a physical device — phones can't reach it.
# Find your IP: ipconfig getifaddr en0 (Mac) or hostname -I (Linux)
EXPO_PUBLIC_API_URL=http://192.168.1.42:8000
